# MATS: Memory-Aware Tiered Storage

Replication package for MATS evaluation.

## Structure
- `configs/` — Caliper benchmark configurations
- `scripts/` — Run and analysis scripts
- `results/` — GC event and latency summary CSVs
- `src/` — Smart contract source
